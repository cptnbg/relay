# Probe / session scratch output
*.out
*.err
.DS_Store

# Babysitter plugin runtime state — never part of this repo
.a5c/

# Nothing generated by a test run belongs in git
src/step*.txt

# test/run.sh builds a throwaway relay state dir here. Relay stages untracked
# non-ignored files, so without this line a test run gets auto-committed.
/state/
