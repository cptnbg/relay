---
description: Report what the current relay run is doing.
argument-hint: ""
---

Invoke the `relay` skill (Skill tool) and follow its `status` section.

Read-only. Summarise as prose readable on a phone, not a data dump.
